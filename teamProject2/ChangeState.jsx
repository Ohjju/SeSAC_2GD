// // TodoDropdown에서 state(입력받은 stateColor) 받아와서 TodoBoX style 변경
// export const ChangeState = (inputstate: any): void => {
//     // .todoState가 가장 첫번째 생성된 할일에만 적용됨.. 상태관리 필요??
//     const current = document.querySelector(".todoState") as HTMLInputElement;
//     current.style.background = `var(${inputstate})`;
//   };
